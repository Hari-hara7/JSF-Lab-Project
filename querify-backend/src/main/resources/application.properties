spring.application.name=QuerifyAI
server.port=8080

# --- PostgreSQL (Neon) ---
spring.datasource.url=
spring.datasource.username=

spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# --- Gemini Flash 2.5 config ---
gemini.api.url=
gemini.api.key=

# --- Custom config ---
querify.query.defaultLimit=500
